package org.iskon.services;

import java.util.List;

import org.iskon.models.TeamUser;
import org.iskon.models.TeamUserSearch;
import org.iskon.models.UserTemple;
import org.iskon.models.UserTempleSearch;


public interface UserTempleService {

	//List<UserTemple> addUserTemple(List<UserTemple> listUsertemple);
	
	
	  //List<UserTemple> getUserTemple(UserTempleSearch query);
	  
	  //UserTemple updateUserTemple(UserTemple usertemple);
	  
	  //void deleteUserTemple(List<UserTemple> newUserTempleMapping);
	  
	  //List<UserTemple> getUserTempleWithDescription();
	 
	
	
}

